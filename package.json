{
  "name": "invert-tree",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "npx tsc",
    "start": "npm run build && node ./dist/index.js",
    "docs": "npx typedoc src/"
  },
  "devDependencies": {
    "typescript": "^5.4.3"
  }
}
